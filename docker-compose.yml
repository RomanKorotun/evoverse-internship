services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: node_app_image
    container_name: node_app
    volumes:
      - .:/app
      - hw-1-node-modules:/app/node_modules
    ports:
      - "3030:3030"

volumes:
  hw-1-node-modules:
